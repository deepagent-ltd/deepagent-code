---
title: CLI
description: deepagent-code CLI seçenekleri ve komutları.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

deepagent-code CLI herhangi bir argüman olmadan çalıştırıldığında varsayılan olarak [TUI](/docs/tui)'yi başlatır.

```bash
deepagent-code
```

Ancak bu sayfada belgelendiği gibi komutları da kabul eder. Bu, deepagent-code ile programlı olarak etkileşim kurmanıza olanak tanır.

```bash
deepagent-code run "Explain how closures work in JavaScript"
```

---

### tui

deepagent-code terminal kullanıcı arayüzünü başlatır.

```bash
deepagent-code [project]
```

#### Bayraklar

| Bayrak                                   | Kısa | Açıklama                                                                    |
| ---------------------------------------- | ---- | --------------------------------------------------------------------------- |
| <nobr><code>{"--continue"}</code></nobr> | `-c` | Son oturuma devam et                                                        |
| <nobr><code>{"--session"}</code></nobr>  | `-s` | Devam edecek oturum kimliği                                                 |
| <nobr><code>{"--fork"}</code></nobr>     |      | Devam ederken oturumu fork'lar (`--continue` veya `--session` ile kullanın) |
| <nobr><code>{"--prompt"}</code></nobr>   |      | Kullanılacak prompt                                                         |
| <nobr><code>{"--model"}</code></nobr>    | `-m` | provider/model biçiminde kullanılacak model                                 |
| <nobr><code>{"--agent"}</code></nobr>    |      | Kullanılacak agent                                                          |
| <nobr><code>{"--port"}</code></nobr>     |      | Dinlenecek port                                                             |
| <nobr><code>{"--hostname"}</code></nobr> |      | Dinlenecek host adı                                                         |

---

## Komutlar

deepagent-code CLI ayrıca aşağıdaki komutlara da sahiptir.

---

### agent

deepagent-code için agent'ları yönetin.

```bash
deepagent-code agent [command]
```

---

### attach

`serve` veya `web` komutlarıyla başlatılan, halihazırda çalışan bir deepagent-code arka uç sunucusuna bir terminal ekleyin.

```bash
deepagent-code attach [url]
```

Bu, TUI öğesinin uzak bir deepagent-code arka ucuyla kullanılmasına olanak tanır. Örneğin:

```bash
# Start the backend server for web/mobile access
deepagent-code web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
deepagent-code attach http://10.20.30.40:4096
```

#### Bayraklar

| Bayrak                                   | Kısa | Tanım                                                                                         |
| ---------------------------------------- | ---- | --------------------------------------------------------------------------------------------- |
| <nobr><code>{"--dir"}</code></nobr>      |      | TUI'yi başlatmak için çalışma dizini                                                          |
| <nobr><code>{"--continue"}</code></nobr> | `-c` | Son oturuma devam et                                                                          |
| <nobr><code>{"--session"}</code></nobr>  | `-s` | Devam edecek oturum açma bilgileri                                                            |
| <nobr><code>{"--fork"}</code></nobr>     |      | Devam ederken oturumu çatalla (`--continue` veya `--session` ile kullanın)                    |
| <nobr><code>{"--password"}</code></nobr> | `-p` | Temel kimlik doğrulama parolası (varsayılan: `DEEPAGENT_CODE_SERVER_PASSWORD`)                      |
| <nobr><code>{"--username"}</code></nobr> | `-u` | Temel kimlik doğrulama kullanıcı adı (varsayılan: `DEEPAGENT_CODE_SERVER_USERNAME` veya `deepagent-code`) |

---

#### create

Özel yapılandırmaya sahip yeni bir agent oluşturun.

```bash
deepagent-code agent create
```

Bu komut, özel sistem istemi ve araç yapılandırmasıyla yeni bir agent oluşturma konusunda size yol gösterecektir.

---

#### list

Mevcut tüm agent'ları listeleyin.

```bash
deepagent-code agent list
```

---

### auth

Sağlayıcılar için kimlik bilgilerini ve oturum açmayı yönetme komutu.

```bash
deepagent-code auth [command]
```

---

#### login

deepagent-code, [Models.dev](https://models.dev) adresindeki sağlayıcı listesi tarafından desteklenmektedir, dolayısıyla kullanmak istediğiniz herhangi bir sağlayıcı için API anahtarlarını ayarlamak üzere `deepagent-code auth login` kullanabilirsiniz. Bu `~/.deepagent/code/auth.json`'de saklanıyor.

```bash
deepagent-code auth login
```

deepagent-code başlatıldığında sağlayıcıları kimlik bilgileri dosyasından yükler. Ayrıca ortam değişkenlerindeki veya projenizdeki `.env` dosyasındaki anahtarları da kullanır.

---

#### list

Kimlik bilgileri dosyasında depolanan tüm kimliği doğrulanmış sağlayıcıları listeler.

```bash
deepagent-code auth list
```

Veya kısa versiyonu.

```bash
deepagent-code auth ls
```

---

#### logout

Bir sağlayıcıyı kimlik bilgileri dosyasından temizleyerek oturumunuzu kapatır.

```bash
deepagent-code auth logout
```

---

### github

Depo otomasyonu için GitHub aracısını yönetin.

```bash
deepagent-code github [command]
```

---

#### install

GitHub aracısını deponuza yükleyin.

```bash
deepagent-code github install
```

Bu komut gerekli GitHub Actions workflow'unu kurar ve yapılandırma adımlarında size rehberlik eder. [Daha fazla bilgi](/docs/github).

---

#### run

GitHub aracısını çalıştırın. Bu genellikle GitHub Eylemlerinde kullanılır.

```bash
deepagent-code github run
```

##### Bayraklar

| Bayrak                                | Açıklama                                    |
| ------------------------------------- | ------------------------------------------- |
| <nobr><code>{"--event"}</code></nobr> | Aracıyı çalıştırmak için GitHub sahte olayı |
| <nobr><code>{"--token"}</code></nobr> | GitHub personal access token                |

---

### mcp

Model Bağlam Protokolü sunucularını yönetin.

```bash
deepagent-code mcp [command]
```

---

#### add

Yapılandırmanıza bir MCP sunucusu ekleyin.

```bash
deepagent-code mcp add
```

Bu komut, yerel veya uzak bir MCP sunucusu ekleme konusunda size yol gösterecektir.

---

#### list

Yapılandırılmış tüm MCP sunucularını ve bağlantı durumlarını listeleyin.

```bash
deepagent-code mcp list
```

Veya kısa versiyonunu kullanın.

```bash
deepagent-code mcp ls
```

---

#### auth

OAuth'un etkin olduğu bir MCP sunucusuyla kimlik doğrulaması yapın.

```bash
deepagent-code mcp auth [name]
```

Sunucu adı belirtmezseniz mevcut OAuth özellikli sunucular arasından seçim yapmanız istenir.

Ayrıca OAuth özellikli sunucuları ve bunların kimlik doğrulama durumlarını da listeleyebilirsiniz.

```bash
deepagent-code mcp auth list
```

Veya kısa versiyonunu kullanın.

```bash
deepagent-code mcp auth ls
```

---

#### logout

MCP sunucusu için OAuth kimlik bilgilerini kaldırın.

```bash
deepagent-code mcp logout [name]
```

---

#### debug

MCP sunucusu için OAuth bağlantı sorunlarının hatalarını ayıklayın.

```bash
deepagent-code mcp debug <name>
```

---

### models

Yapılandırılmış sağlayıcıların tüm mevcut modellerini listeleyin.

```bash
deepagent-code models [provider]
```

Bu komut, yapılandırdığınız sağlayıcılardaki modelleri `provider/model` formatında listeler.

Bu, [config dosyanızda](/docs/config/) kullanmanız gereken tam model adını bulmak için kullanışlıdır.

Modelleri bu sağlayıcıya göre filtrelemek için isteğe bağlı olarak bir sağlayıcı kimliğini iletebilirsiniz.

```bash
deepagent-code models anthropic
```

#### Bayraklar

| Bayrak                                  | Tanım                                                                       |
| --------------------------------------- | --------------------------------------------------------------------------- |
| <nobr><code>{"--refresh"}</code></nobr> | Modeller.dev'den model önbelleğini yenileyin                                |
| <nobr><code>{"--verbose"}</code></nobr> | Daha ayrıntılı model çıktısı kullanın (maliyetler gibi meta veriler içerir) |

Önbelleğe alınan model listesini güncellemek için `--refresh` bayrağını kullanın. Bu, bir sağlayıcıya yeni modeller eklendiğinde ve bunları deepagent-code'da görmek istediğinizde kullanışlıdır.

```bash
deepagent-code models --refresh
```

---

### run

Doğrudan bir istem ileterek deepagent-code'u etkileşimli olmayan modda çalıştırın.

```bash
deepagent-code run [message..]
```

Bu, komut dosyası oluşturma, otomasyon veya TUI'un tamamını başlatmadan hızlı bir yanıt istediğinizde kullanışlıdır. Örneğin.

```bash "deepagent-code run"
deepagent-code run Explain the use of context in Go
```

Ayrıca, her çalıştırmada MCP sunucusunun soğuk önyükleme sürelerini önlemek için çalışan bir `deepagent-code serve` örneğine de ekleyebilirsiniz:

```bash
# Start a headless server in one terminal
deepagent-code serve

# In another terminal, run commands that attach to it
deepagent-code run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Bayraklar

| Bayrak                                   | Kısa | Açıklama                                                                                      |
| ---------------------------------------- | ---- | --------------------------------------------------------------------------------------------- |
| <nobr><code>{"--command"}</code></nobr>  |      | Çalıştırılacak komut, args için mesajı kullanın                                               |
| <nobr><code>{"--continue"}</code></nobr> | `-c` | Son oturuma devam et                                                                          |
| <nobr><code>{"--session"}</code></nobr>  | `-s` | Devam edecek oturum kimliği                                                                   |
| <nobr><code>{"--fork"}</code></nobr>     |      | Devam ederken oturumu fork'lar (`--continue` veya `--session` ile kullanın)                   |
| <nobr><code>{"--share"}</code></nobr>    |      | Oturumu paylaşın                                                                              |
| <nobr><code>{"--model"}</code></nobr>    | `-m` | provider/model biçiminde kullanılacak model                                                   |
| <nobr><code>{"--agent"}</code></nobr>    |      | Kullanılacak temsilci                                                                         |
| <nobr><code>{"--file"}</code></nobr>     | `-f` | Mesaja eklenecek dosya(lar)                                                                   |
| <nobr><code>{"--format"}</code></nobr>   |      | Biçim: varsayılan (biçimlendirilmiş) veya json (ham JSON olayları)                            |
| <nobr><code>{"--title"}</code></nobr>    |      | Oturumun başlığı (değer sağlanmazsa kısaltılmış bilgi istemi kullanılır)                      |
| <nobr><code>{"--attach"}</code></nobr>   |      | Çalışan bir deepagent-code sunucusuna ekleyin (ör. http://localhost:4096)                           |
| <nobr><code>{"--password"}</code></nobr> | `-p` | Temel kimlik doğrulama parolası (varsayılan: `DEEPAGENT_CODE_SERVER_PASSWORD`)                      |
| <nobr><code>{"--username"}</code></nobr> | `-u` | Temel kimlik doğrulama kullanıcı adı (varsayılan: `DEEPAGENT_CODE_SERVER_USERNAME` veya `deepagent-code`) |
| <nobr><code>{"--dir"}</code></nobr>      |      | Çalıştırılacak dizin veya bağlanırken uzak sunucudaki yol                                     |
| <nobr><code>{"--variant"}</code></nobr>  |      | Model varyantı (sağlayıcıya özgü muhakeme düzeyi)                                             |
| <nobr><code>{"--thinking"}</code></nobr> |      | Düşünme bloklarını göster                                                                     |
| <nobr><code>{"--port"}</code></nobr>     |      | Yerel sunucunun bağlantı noktası (varsayılan olarak rastgele bağlantı noktasıdır)             |

---

### serve

API erişimi için headless bir deepagent-code sunucusu başlatır. Tam HTTP arayüzü için [server docs](/docs/server) sayfasına bakın.

```bash
deepagent-code serve
```

Bu, TUI arayüzü olmadan deepagent-code işlevselliğine API erişimi sağlayan bir HTTP sunucusunu başlatır. HTTP temel kimlik doğrulamasını etkinleştirmek için `DEEPAGENT_CODE_SERVER_PASSWORD` öğesini ayarlayın (kullanıcı adı varsayılan olarak `deepagent-code` şeklindedir).

#### Bayraklar

| Bayrak                                   | Tanım                                      |
| ---------------------------------------- | ------------------------------------------ |
| <nobr><code>{"--port"}</code></nobr>     | Dinlenecek bağlantı noktası                |
| <nobr><code>{"--hostname"}</code></nobr> | Dinlenecek ana bilgisayar adı              |
| <nobr><code>{"--mdns"}</code></nobr>     | mDNS bulmayı etkinleştir                   |
| <nobr><code>{"--cors"}</code></nobr>     | CORS'a izin verecek ek tarayıcı kaynakları |

---

### session

deepagent-code oturumlarını yönetin.

```bash
deepagent-code session [command]
```

---

#### list

Tüm deepagent-code oturumlarını listeleyin.

```bash
deepagent-code session list
```

##### Bayraklar

| Bayrak                                    | Kısa | Tanım                                  |
| ----------------------------------------- | ---- | -------------------------------------- |
| <nobr><code>{"--max-count"}</code></nobr> | `-n` | En son N oturumla sınırla              |
| <nobr><code>{"--format"}</code></nobr>    |      | Çıkış formatı: tablo veya json (tablo) |

---

### stats

deepagent-code oturumlarınız için belirteç kullanımı ve maliyet istatistiklerini gösterin.

```bash
deepagent-code stats
```

#### Bayraklar

| Bayrak                                  | Açıklama                                                                                                    |
| --------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| <nobr><code>{"--days"}</code></nobr>    | Son N güne ait istatistikleri göster (tüm zamanlar)                                                         |
| <nobr><code>{"--tools"}</code></nobr>   | Gösterilecek araç sayısı (tümü)                                                                             |
| <nobr><code>{"--models"}</code></nobr>  | Model kullanım dökümünü göster (varsayılan olarak gizlidir). En üstteki N'yi göstermek için bir sayı iletin |
| <nobr><code>{"--project"}</code></nobr> | Projeye göre filtrele (tüm projeler, boş değer: mevcut proje)                                               |

---

### export

Oturum verilerini JSON olarak dışa aktarın.

```bash
deepagent-code export [sessionID]
```

Bir oturum kimliği sağlamazsanız mevcut oturumlar arasından seçim yapmanız istenir.

---

### import

Bir JSON dosyasından veya deepagent-code paylaşımından URL oturum verilerini içe aktarın.

```bash
deepagent-code import <file>
```

Yerel bir dosyadan veya deepagent-code paylaşımından (URL) içe aktarabilirsiniz.

```bash
deepagent-code import session.json
deepagent-code import https://opncd.ai/s/abc123
```

---

### web

Web arayüzüyle başsız bir deepagent-code sunucusu başlatın.

```bash
deepagent-code web
```

Bu, bir HTTP sunucusunu başlatır ve bir web arayüzü aracılığıyla deepagent-code'a erişmek için bir web tarayıcısı açar. HTTP temel kimlik doğrulamasını etkinleştirmek için `DEEPAGENT_CODE_SERVER_PASSWORD` öğesini ayarlayın (kullanıcı adı varsayılan olarak `deepagent-code` şeklindedir).

#### Bayraklar

| Bayrak                                   | Tanım                                      |
| ---------------------------------------- | ------------------------------------------ |
| <nobr><code>{"--port"}</code></nobr>     | Dinlenecek bağlantı noktası                |
| <nobr><code>{"--hostname"}</code></nobr> | Dinlenecek ana bilgisayar adı              |
| <nobr><code>{"--mdns"}</code></nobr>     | mDNS bulmayı etkinleştir                   |
| <nobr><code>{"--cors"}</code></nobr>     | CORS'a izin verecek ek tarayıcı kaynakları |

---

### acp

Bir ACP (Ajan İstemci Protokolü) sunucusu başlatın.

```bash
deepagent-code acp
```

Bu komut, nd-JSON kullanarak stdin/stdout aracılığıyla iletişim kuran bir ACP sunucusunu başlatır.

#### Bayraklar

| Bayrak                                   | Açıklama            |
| ---------------------------------------- | ------------------- |
| <nobr><code>{"--cwd"}</code></nobr>      | Çalışma dizini      |
| <nobr><code>{"--port"}</code></nobr>     | Dinlenecek port     |
| <nobr><code>{"--hostname"}</code></nobr> | Dinlenecek host adı |

---

### uninstall

deepagent-code'u kaldırın ve ilgili tüm dosyaları kaldırın.

```bash
deepagent-code uninstall
```

#### Bayraklar

| Bayrak                                      | Kısa | Tanım                                           |
| ------------------------------------------- | ---- | ----------------------------------------------- |
| <nobr><code>{"--keep-config"}</code></nobr> | `-c` | Yapılandırma dosyalarını sakla                  |
| <nobr><code>{"--keep-data"}</code></nobr>   | `-d` | Oturum verilerini ve anlık görüntüleri saklayın |
| <nobr><code>{"--dry-run"}</code></nobr>     |      | Nelerin kaldırılmadan kaldırılacağı göster      |
| <nobr><code>{"--force"}</code></nobr>       | `-f` | Onay istemlerini atla                           |

---

### upgrade

deepagent-code'u en son sürüme veya belirli bir sürüme günceller.

```bash
deepagent-code upgrade [target]
```

En son sürüme yükseltmek için.

```bash
deepagent-code upgrade
```

Belirli bir sürüme yükseltmek için:

```bash
deepagent-code upgrade v0.1.48
```

#### Bayraklar

| Bayrak                                 | Kısa | Açıklama                                               |
| -------------------------------------- | ---- | ------------------------------------------------------ |
| <nobr><code>{"--method"}</code></nobr> | `-m` | Kullanılan kurulum yöntemi: curl, npm, pnpm, bun, brew |

---

## Global bayraklar

deepagent-code CLI aşağıdaki global bayrakları destekler.

| Bayrak                                     | Kısa | Tanım                                    |
| ------------------------------------------ | ---- | ---------------------------------------- |
| <nobr><code>{"--help"}</code></nobr>       | `-h` | Yardımı görüntüle                        |
| <nobr><code>{"--version"}</code></nobr>    | `-v` | Sürüm numarasını yazdır                  |
| <nobr><code>{"--print-logs"}</code></nobr> |      | Günlükleri stderr'e yazdır               |
| <nobr><code>{"--log-level"}</code></nobr>  |      | Günlük düzeyi (DEBUG, INFO, WARN, ERROR) |

---

## Ortam değişkenleri

deepagent-code ortam değişkenleri kullanılarak yapılandırılabilir.

| Değişken                              | Tip     | Açıklama                                                                    |
| ------------------------------------- | ------- | --------------------------------------------------------------------------- |
| `DEEPAGENT_CODE_AUTO_SHARE`                 | boolean | Oturumları otomatik olarak paylaş                                           |
| `DEEPAGENT_CODE_GIT_BASH_PATH`              | string  | Windows'ta yürütülebilir Git Bash'in Yolu                                   |
| `DEEPAGENT_CODE_CONFIG`                     | string  | Yapılandırma dosyasının yolu                                                |
| `DEEPAGENT_CODE_TUI_CONFIG`                 | string  | TUI yapılandırma dosyasının yolu                                            |
| `DEEPAGENT_CODE_CONFIG_DIR`                 | string  | Yapılandırma dizinine giden yol                                             |
| `DEEPAGENT_CODE_CONFIG_CONTENT`             | string  | Satır içi JSON config içeriği                                               |
| `DEEPAGENT_CODE_DISABLE_AUTOUPDATE`         | boolean | Otomatik güncelleme kontrollerini devre dışı bırak                          |
| `DEEPAGENT_CODE_DISABLE_PRUNE`              | boolean | Eski verilerin temizlenmesini devre dışı bırak                              |
| `DEEPAGENT_CODE_DISABLE_TERMINAL_TITLE`     | boolean | Otomatik terminal başlığı güncellemelerini devre dışı bırakın               |
| `DEEPAGENT_CODE_PERMISSION`                 | string  | Satır içi JSON izin config'i                                                |
| `DEEPAGENT_CODE_DISABLE_DEFAULT_PLUGINS`    | boolean | Varsayılan eklentileri devre dışı bırakın                                   |
| `DEEPAGENT_CODE_DISABLE_LSP_DOWNLOAD`       | boolean | Otomatik LSP sunucu indirmelerini devre dışı bırakın                        |
| `DEEPAGENT_CODE_ENABLE_EXPERIMENTAL_MODELS` | boolean | Deneysel modelleri etkinleştir                                              |
| `DEEPAGENT_CODE_DISABLE_AUTOCOMPACT`        | boolean | Otomatik context sıkıştırmayı devre dışı bırak                              |
| `DEEPAGENT_CODE_DISABLE_CLAUDE_CODE`        | boolean | `.claude`'den okumayı devre dışı bırak (istem + beceriler)                  |
| `DEEPAGENT_CODE_DISABLE_CLAUDE_CODE_PROMPT` | boolean | `~/.claude/CLAUDE.md` dosyasını okumayı devre dışı bırak                    |
| `DEEPAGENT_CODE_DISABLE_CLAUDE_CODE_SKILLS` | boolean | `.claude/skills` yüklemesini devre dışı bırak                               |
| `DEEPAGENT_CODE_DISABLE_MODELS_FETCH`       | boolean | Uzak kaynaklardan model getirmeyi devre dışı bırakın                        |
| `DEEPAGENT_CODE_FAKE_VCS`                   | string  | Test amaçlı sahte VCS sağlayıcısı                                           |
| `DEEPAGENT_CODE_CLIENT`                     | string  | Client kimliği (varsayılan: `cli`)                                          |
| `DEEPAGENT_CODE_ENABLE_EXA`                 | boolean | Exa web arama araçlarını etkinleştir                                        |
| `DEEPAGENT_CODE_SERVER_PASSWORD`            | string  | `serve`/`web` için temel kimlik doğrulamayı etkinleştirin                   |
| `DEEPAGENT_CODE_SERVER_USERNAME`            | string  | Temel kimlik doğrulama kullanıcı adını geçersiz kıl (varsayılan `deepagent-code`) |
| `DEEPAGENT_CODE_MODELS_URL`                 | string  | Model yapılandırmasını almak için özel URL                                  |

---

### Deneysel

Bu ortam değişkenleri değişebilecek veya kaldırılabilecek deneysel özellikleri etkinleştirir.

| Değişken                                        | Tip     | Tanım                                                   |
| ----------------------------------------------- | ------- | ------------------------------------------------------- |
| `DEEPAGENT_CODE_EXPERIMENTAL`                         | boolean | Şemsiye bayrağa bağlı deneysel özellikleri etkinleştir  |
| `DEEPAGENT_CODE_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Simge bulmayı etkinleştir                               |
| `DEEPAGENT_CODE_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | TUI'da seçim yapıldığında kopyalamayı devre dışı bırak  |
| `DEEPAGENT_CODE_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | MS cinsinden bash komutları için varsayılan zaman aşımı |
| `DEEPAGENT_CODE_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | LLM yanıtları için maksimum çıktı belirteçleri          |
| `DEEPAGENT_CODE_EXPERIMENTAL_FILEWATCHER`             | boolean | Tüm dizin için dosya izleyiciyi etkinleştir             |
| `DEEPAGENT_CODE_EXPERIMENTAL_OXFMT`                   | boolean | Oxfmt biçimlendiriciyi etkinleştir                      |
| `DEEPAGENT_CODE_EXPERIMENTAL_LSP_TOOL`                | boolean | Deneysel LSP aracını etkinleştir                        |
| `DEEPAGENT_CODE_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Dosya izleyiciyi devre dışı bırak                       |
| `DEEPAGENT_CODE_EXPERIMENTAL_EXA`                     | boolean | Deneysel Exa özelliklerini etkinleştirin                |
| `DEEPAGENT_CODE_EXPERIMENTAL_LSP_TY`                  | boolean | python dosyaları için TY LSP'yi etkinleştir             |
| `DEEPAGENT_CODE_EXPERIMENTAL_PLAN_MODE`               | boolean | Plan modunu etkinleştir                                 |
| `DEEPAGENT_CODE_EXPERIMENTAL_BACKGROUND_SUBAGENTS`    | boolean | Arka plan alt ajan görevlerini etkinleştir              |
| `DEEPAGENT_CODE_EXPERIMENTAL_EVENT_SYSTEM`            | boolean | Deneysel olay sistemini etkinleştir                     |
| `DEEPAGENT_CODE_EXPERIMENTAL_NATIVE_LLM`              | boolean | Yerel LLM istek yolunu etkinleştir                      |
| `DEEPAGENT_CODE_EXPERIMENTAL_PARALLEL`                | boolean | Paralel web araması yürütmesini etkinleştir             |
| `DEEPAGENT_CODE_EXPERIMENTAL_SCOUT`                   | boolean | Scout alt ajanını etkinleştir                           |
| `DEEPAGENT_CODE_EXPERIMENTAL_WORKSPACES`              | boolean | Çalışma alanı desteğini etkinleştir                     |
