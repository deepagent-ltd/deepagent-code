---
title: CLI
description: DeepAgent Code CLI-alternativer og kommandoer.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

DeepAgent Code CLI starter som standard [TUI](/docs/tui) når den kjøres uten noen argumenter.

```bash
deepagent-code
```

Men den godtar også kommandoer som dokumentert på denne siden. Dette lar deg samhandle med DeepAgent Code programmatisk.

```bash
deepagent-code run "Explain how closures work in JavaScript"
```

---

### tui

Start DeepAgent Code-terminalbrukergrensesnittet.

```bash
deepagent-code [project]
```

#### Flagg

| Flagg                                    | Kort | Beskrivelse                                                              |
| ---------------------------------------- | ---- | ------------------------------------------------------------------------ |
| <nobr><code>{"--continue"}</code></nobr> | `-c` | Fortsett siste økt                                                       |
| <nobr><code>{"--session"}</code></nobr>  | `-s` | Økt ID for å fortsette                                                   |
| <nobr><code>{"--fork"}</code></nobr>     |      | Forgren økten ved fortsettelse (bruk med `--continue` eller `--session`) |
| <nobr><code>{"--prompt"}</code></nobr>   |      | Ledetekst som skal brukes                                                |
| <nobr><code>{"--model"}</code></nobr>    | `-m` | Modell å bruke i form av leverandør/modell                               |
| <nobr><code>{"--agent"}</code></nobr>    |      | Agent som skal brukes                                                    |
| <nobr><code>{"--port"}</code></nobr>     |      | Port å lytte på                                                          |
| <nobr><code>{"--hostname"}</code></nobr> |      | Vertsnavn å lytte på                                                     |

---

## Kommandoer

DeepAgent Code CLI har også følgende kommandoer.

---

### agent

Administrer agenter for DeepAgent Code.

```bash
deepagent-code agent [command]
```

---

### attach

Koble en terminal til en allerede kjørende DeepAgent Code-backend-server startet via `serve` eller `web` kommandoer.

```bash
deepagent-code attach [url]
```

Dette gjør det mulig å bruke TUI med en ekstern DeepAgent Code-backend. For eksempel:

```bash
# Start the backend server for web/mobile access
deepagent-code web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
deepagent-code attach http://10.20.30.40:4096
```

#### Flagg

| Flagg                                    | Kort | Beskrivelse                                                                                        |
| ---------------------------------------- | ---- | -------------------------------------------------------------------------------------------------- |
| <nobr><code>{"--dir"}</code></nobr>      |      | Arbeidskatalog for å starte TUI i                                                                  |
| <nobr><code>{"--continue"}</code></nobr> | `-c` | Fortsett siste økt                                                                                 |
| <nobr><code>{"--session"}</code></nobr>  | `-s` | Økt ID for å fortsette                                                                             |
| <nobr><code>{"--fork"}</code></nobr>     |      | Forgren økten ved fortsettelse (bruk med `--continue` eller `--session`)                           |
| <nobr><code>{"--password"}</code></nobr> | `-p` | Passord for grunnleggende autentisering (standard: `DEEPAGENT_CODE_SERVER_PASSWORD`)                     |
| <nobr><code>{"--username"}</code></nobr> | `-u` | Brukernavn for grunnleggende autentisering (standard: `DEEPAGENT_CODE_SERVER_USERNAME` eller `deepagent-code`) |

---

#### create

Opprett en ny agent med tilpasset konfigurasjon.

```bash
deepagent-code agent create
```

Denne kommandoen vil veilede deg gjennom å lage en ny agent med en tilpasset systemmelding og verktøykonfigurasjon.

---

#### list

List opp alle tilgjengelige agenter.

```bash
deepagent-code agent list
```

---

### auth

Kommando for administrasjon av påloggingsinformasjon og innlogging for leverandører.

```bash
deepagent-code auth [command]
```

---

#### login

DeepAgent Code drives av leverandørlisten på [Models.dev](https://models.dev), så du kan bruke `deepagent-code auth login` til å konfigurere API-nøkler for enhver leverandør du vil bruke. Dette er lagret i `~/.deepagent/code/auth.json`.

```bash
deepagent-code auth login
```

Når DeepAgent Code starter opp, laster den leverandørene fra filen med påloggingsinformasjon. Og hvis det er noen nøkler definert i miljøene dine eller en `.env`-fil i prosjektet ditt.

---

#### list

Viser alle de autentiserte leverandørene som er lagret i filen med påloggingsinformasjon.

```bash
deepagent-code auth list
```

Eller bruk kortversjonen.

```bash
deepagent-code auth ls
```

---

#### logout

Logger deg ut av en leverandør ved å fjerne den fra filen med påloggingsinformasjon.

```bash
deepagent-code auth logout
```

---

### github

Administrer GitHub-agenten for depotautomatisering.

```bash
deepagent-code github [command]
```

---

#### install

Installer GitHub-agenten i depotet ditt.

```bash
deepagent-code github install
```

Dette setter opp den nødvendige GitHub Actions-arbeidsflyten og veileder deg gjennom konfigurasjonsprosessen. [Finn ut mer](/docs/github).

---

#### run

Kjør GitHub-agenten. Dette brukes vanligvis i GitHub Actions.

```bash
deepagent-code github run
```

##### Flagg

| Flagg                                 | Beskrivelse                                  |
| ------------------------------------- | -------------------------------------------- |
| <nobr><code>{"--event"}</code></nobr> | GitHub mock-hendelse agenten skal kjøres for |
| <nobr><code>{"--token"}</code></nobr> | GitHub personlig tilgangsnøkkel              |

---

### mcp

Administrer Model Context Protocol-servere.

```bash
deepagent-code mcp [command]
```

---

#### add

Legg til en MCP-server til konfigurasjonen.

```bash
deepagent-code mcp add
```

Denne kommandoen vil veilede deg gjennom å legge til enten en lokal eller ekstern MCP-server.

---

#### list

Liste alle konfigurerte MCP-servere og deres tilkoblingsstatus.

```bash
deepagent-code mcp list
```

Eller bruk den korte versjonen.

```bash
deepagent-code mcp ls
```

---

#### auth

Autentiser med en OAuth-aktivert MCP-server.

```bash
deepagent-code mcp auth [name]
```

Hvis du ikke oppgir et servernavn, blir du bedt om å velge blant tilgjengelige OAuth-kompatible servere.

Du kan også liste opp OAuth-kompatible servere og deres autentiseringsstatus.

```bash
deepagent-code mcp auth list
```

Eller bruk den korte versjonen.

```bash
deepagent-code mcp auth ls
```

---

#### logout

Fjern OAuth-påloggingsinformasjon for en MCP-server.

```bash
deepagent-code mcp logout [name]
```

---

#### debug

Feilsøk OAuth-tilkoblingsproblemer for en MCP-server.

```bash
deepagent-code mcp debug <name>
```

---

### models

List opp alle tilgjengelige modeller fra konfigurerte leverandører.

```bash
deepagent-code models [provider]
```

Denne kommandoen viser alle modeller som er tilgjengelige på tvers av dine konfigurerte leverandører i formatet `provider/model`.

Dette er nyttig for å finne ut det eksakte modellnavnet som skal brukes i [din konfigurasjon](/docs/config/).

Du kan eventuelt sende en leverandør ID for å filtrere modeller etter den leverandøren.

```bash
deepagent-code models anthropic
```

#### Flagg

| Flagg                                   | Beskrivelse                                                         |
| --------------------------------------- | ------------------------------------------------------------------- |
| <nobr><code>{"--refresh"}</code></nobr> | Oppdater modellbufferen fra models.dev                              |
| <nobr><code>{"--verbose"}</code></nobr> | Bruk mer detaljert modellutdata (inkluderer metadata som kostnader) |

Bruk `--refresh`-flagget for å oppdatere den bufrede modelllisten. Dette er nyttig når nye modeller er lagt til en leverandør og du vil se dem i DeepAgent Code.

```bash
deepagent-code models --refresh
```

---

### run

Kjør DeepAgent Code i ikke-interaktiv modus ved å sende en melding direkte.

```bash
deepagent-code run [message..]
```

Dette er nyttig for skripting, automatisering eller når du vil ha et raskt svar uten å starte hele TUI. For eksempel.

```bash "deepagent-code run"
deepagent-code run Explain the use of context in Go
```

Du kan også koble til en kjørende `deepagent-code serve`-forekomst for å unngå MCP-serveres kaldstarttid ved hver kjøring:

```bash
# Start a headless server in one terminal
deepagent-code serve

# In another terminal, run commands that attach to it
deepagent-code run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Flagg

| Flagg                                    | Kort | Beskrivelse                                                                                        |
| ---------------------------------------- | ---- | -------------------------------------------------------------------------------------------------- |
| <nobr><code>{"--command"}</code></nobr>  |      | Kommandoen for å kjøre, bruk melding for args                                                      |
| <nobr><code>{"--continue"}</code></nobr> | `-c` | Fortsett siste økt                                                                                 |
| <nobr><code>{"--session"}</code></nobr>  | `-s` | Økt ID for å fortsette                                                                             |
| <nobr><code>{"--fork"}</code></nobr>     |      | Forgren økten ved fortsettelse (bruk med `--continue` eller `--session`)                           |
| <nobr><code>{"--share"}</code></nobr>    |      | Del økten                                                                                          |
| <nobr><code>{"--model"}</code></nobr>    | `-m` | Modell å bruke i form av leverandør/modell                                                         |
| <nobr><code>{"--agent"}</code></nobr>    |      | Agent å bruke                                                                                      |
| <nobr><code>{"--file"}</code></nobr>     | `-f` | Fil(er) som skal legges ved meldingen                                                              |
| <nobr><code>{"--format"}</code></nobr>   |      | Format: standard (formatert) eller json (rå JSON hendelser)                                        |
| <nobr><code>{"--title"}</code></nobr>    |      | Tittel for økten (bruker avkortet ledetekst hvis ingen verdi er oppgitt)                           |
| <nobr><code>{"--attach"}</code></nobr>   |      | Koble til en kjørende DeepAgent Code-server (f.eks. http://localhost:4096)                               |
| <nobr><code>{"--password"}</code></nobr> | `-p` | Passord for grunnleggende autentisering (standard: `DEEPAGENT_CODE_SERVER_PASSWORD`)                     |
| <nobr><code>{"--username"}</code></nobr> | `-u` | Brukernavn for grunnleggende autentisering (standard: `DEEPAGENT_CODE_SERVER_USERNAME` eller `deepagent-code`) |
| <nobr><code>{"--dir"}</code></nobr>      |      | Katalog å kjøre i, eller sti på fjernserveren ved tilkobling                                       |
| <nobr><code>{"--variant"}</code></nobr>  |      | Modellvariant (leverandørspesifikk resonneringsinnsats)                                            |
| <nobr><code>{"--thinking"}</code></nobr> |      | Vis tenkeblokker                                                                                   |
| <nobr><code>{"--port"}</code></nobr>     |      | Port for den lokale serveren (standard til tilfeldig port)                                         |

---

### serve

Start en headless DeepAgent Code-server for API-tilgang. Sjekk ut [server-dokumentene](/docs/server) for hele HTTP-grensesnittet.

```bash
deepagent-code serve
```

Dette starter en HTTP-server som gir API tilgang til DeepAgent Code-funksjonalitet uten TUI-grensesnittet. Sett `DEEPAGENT_CODE_SERVER_PASSWORD` for å aktivere HTTP grunnleggende autentisering (brukernavn er standard til `deepagent-code`).

#### Flagg

| Flagg                                    | Beskrivelse                                        |
| ---------------------------------------- | -------------------------------------------------- |
| <nobr><code>{"--port"}</code></nobr>     | Port å lytte på                                    |
| <nobr><code>{"--hostname"}</code></nobr> | Vertsnavn å lytte på                               |
| <nobr><code>{"--mdns"}</code></nobr>     | Aktiver mDNS-oppdagelse                            |
| <nobr><code>{"--cors"}</code></nobr>     | Ytterligere nettleseropprinnelse som tillater CORS |

---

### session

Administrer DeepAgent Code-økter.

```bash
deepagent-code session [command]
```

---

#### list

List opp alle DeepAgent Code-økter.

```bash
deepagent-code session list
```

##### Flagg

| Flagg                                     | Kort | Beskrivelse                              |
| ----------------------------------------- | ---- | ---------------------------------------- |
| <nobr><code>{"--max-count"}</code></nobr> | `-n` | Begrens til N siste økter                |
| <nobr><code>{"--format"}</code></nobr>    |      | Utdataformat: tabell eller json (tabell) |

---

### stats

Vis tokenbruk og kostnadsstatistikk for DeepAgent Code-øktene dine.

```bash
deepagent-code stats
```

#### Flagg

| Flagg                                   | Beskrivelse                                                                      |
| --------------------------------------- | -------------------------------------------------------------------------------- |
| <nobr><code>{"--days"}</code></nobr>    | Vis statistikk for de siste N dagene (hele tiden)                                |
| <nobr><code>{"--tools"}</code></nobr>   | Antall verktøy som skal vises (alle)                                             |
| <nobr><code>{"--models"}</code></nobr>  | Vis oversikt over modellbruk (skjult som standard). Gi et tall for å vise topp N |
| <nobr><code>{"--project"}</code></nobr> | Filtrer etter prosjekt (alle prosjekter, tom streng: gjeldende prosjekt)         |

---

### export

Eksporter øktdata som JSON.

```bash
deepagent-code export [sessionID]
```

Hvis du ikke gir en økt ID, blir du bedt om å velge fra tilgjengelige økter.

---

### import

Importer øktdata fra en JSON-fil eller en DeepAgent Code share-URL.

```bash
deepagent-code import <file>
```

Du kan importere fra en lokal fil eller en DeepAgent Code share-URL.

```bash
deepagent-code import session.json
deepagent-code import https://opncd.ai/s/abc123
```

---

### web

Start en headless DeepAgent Code-server med et webgrensesnitt.

```bash
deepagent-code web
```

Dette starter en HTTP-server og åpner en nettleser for å få tilgang til DeepAgent Code via et nettgrensesnitt. Sett `DEEPAGENT_CODE_SERVER_PASSWORD` for å aktivere HTTP grunnleggende autentisering (brukernavn er standard til `deepagent-code`).

#### Flagg

| Flagg                                    | Beskrivelse                                        |
| ---------------------------------------- | -------------------------------------------------- |
| <nobr><code>{"--port"}</code></nobr>     | Port å lytte på                                    |
| <nobr><code>{"--hostname"}</code></nobr> | Vertsnavn å lytte på                               |
| <nobr><code>{"--mdns"}</code></nobr>     | Aktiver mDNS-oppdagelse                            |
| <nobr><code>{"--cors"}</code></nobr>     | Ytterligere nettleseropprinnelse som tillater CORS |

---

### acp

Start en ACP (Agent Client Protocol) server.

```bash
deepagent-code acp
```

Denne kommandoen starter en ACP-server som kommuniserer via stdin/stdout ved å bruke nd-JSON.

#### Flagg

| Flagg                                    | Beskrivelse          |
| ---------------------------------------- | -------------------- |
| <nobr><code>{"--cwd"}</code></nobr>      | Arbeidskatalog       |
| <nobr><code>{"--port"}</code></nobr>     | Port å lytte på      |
| <nobr><code>{"--hostname"}</code></nobr> | Vertsnavn å lytte på |

---

### uninstall

Avinstaller DeepAgent Code og fjern alle relaterte filer.

```bash
deepagent-code uninstall
```

#### Flagg

| Flagg                                       | Kort | Beskrivelse                                   |
| ------------------------------------------- | ---- | --------------------------------------------- |
| <nobr><code>{"--keep-config"}</code></nobr> | `-c` | Behold konfigurasjonsfiler                    |
| <nobr><code>{"--keep-data"}</code></nobr>   | `-d` | Behold øktdata og øyeblikksbilder             |
| <nobr><code>{"--dry-run"}</code></nobr>     |      | Vis hva som ville blitt fjernet uten å fjerne |
| <nobr><code>{"--force"}</code></nobr>       | `-f` | Hopp over bekreftelsesforespørsler            |

---

### upgrade

Oppdaterer DeepAgent Code til siste versjon eller en spesifikk versjon.

```bash
deepagent-code upgrade [target]
```

For å oppgradere til siste versjon.

```bash
deepagent-code upgrade
```

For å oppgradere til en bestemt versjon.

```bash
deepagent-code upgrade v0.1.48
```

#### Flagg

| Flagg                                  | Kort | Beskrivelse                                                    |
| -------------------------------------- | ---- | -------------------------------------------------------------- |
| <nobr><code>{"--method"}</code></nobr> | `-m` | Installasjonsmetoden som ble brukt: curl, npm, pnpm, bun, brew |

---

## Globale flagg

DeepAgent Code CLI bruker følgende globale flagg.

| Flagg                                      | Kort | Beskrivelse                         |
| ------------------------------------------ | ---- | ----------------------------------- |
| <nobr><code>{"--help"}</code></nobr>       | `-h` | Vis hjelp                           |
| <nobr><code>{"--version"}</code></nobr>    | `-v` | Skriv ut versjonsnummer             |
| <nobr><code>{"--print-logs"}</code></nobr> |      | Skriv ut logger til stderr          |
| <nobr><code>{"--log-level"}</code></nobr>  |      | Loggnivå (DEBUG, INFO, WARN, ERROR) |

---

## Miljøvariabler

DeepAgent Code kan konfigureres ved hjelp av miljøvariabler.

| Variabel                              | Type   | Beskrivelse                                                           |
| ------------------------------------- | ------ | --------------------------------------------------------------------- |
| `DEEPAGENT_CODE_AUTO_SHARE`                 | boolsk | Del økter automatisk                                                  |
| `DEEPAGENT_CODE_GIT_BASH_PATH`              | streng | Bane til Git Bash-kjørbar på Windows                                  |
| `DEEPAGENT_CODE_CONFIG`                     | streng | Bane til konfigurasjonsfil                                            |
| `DEEPAGENT_CODE_TUI_CONFIG`                 | streng | Bane til TUI-konfigurasjonsfil                                        |
| `DEEPAGENT_CODE_CONFIG_DIR`                 | streng | Bane til konfigurasjonskatalog                                        |
| `DEEPAGENT_CODE_CONFIG_CONTENT`             | streng | Innebygd json-konfigurasjonsinnhold                                   |
| `DEEPAGENT_CODE_DISABLE_AUTOUPDATE`         | boolsk | Deaktiver automatiske oppdateringskontroller                          |
| `DEEPAGENT_CODE_DISABLE_PRUNE`              | boolsk | Deaktiver beskjæring av gamle data                                    |
| `DEEPAGENT_CODE_DISABLE_TERMINAL_TITLE`     | boolsk | Deaktiver automatiske terminaltitteloppdateringer                     |
| `DEEPAGENT_CODE_PERMISSION`                 | streng | Innebygd json-tillatelseskonfigurasjon                                |
| `DEEPAGENT_CODE_DISABLE_DEFAULT_PLUGINS`    | boolsk | Deaktiver standard plugins                                            |
| `DEEPAGENT_CODE_DISABLE_LSP_DOWNLOAD`       | boolsk | Deaktiver automatiske LSP servernedlastinger                          |
| `DEEPAGENT_CODE_ENABLE_EXPERIMENTAL_MODELS` | boolsk | Aktiver eksperimentelle modeller                                      |
| `DEEPAGENT_CODE_DISABLE_AUTOCOMPACT`        | boolsk | Deaktiver automatisk kontekstkomprimering                             |
| `DEEPAGENT_CODE_DISABLE_CLAUDE_CODE`        | boolsk | Deaktiver lesing fra `.claude` (spørsmål + ferdigheter)               |
| `DEEPAGENT_CODE_DISABLE_CLAUDE_CODE_PROMPT` | boolsk | Deaktiver lesing `~/.claude/CLAUDE.md`                                |
| `DEEPAGENT_CODE_DISABLE_CLAUDE_CODE_SKILLS` | boolsk | Deaktiver innlasting av `.claude/skills`                              |
| `DEEPAGENT_CODE_DISABLE_MODELS_FETCH`       | boolsk | Deaktiver henting av modeller fra eksterne kilder                     |
| `DEEPAGENT_CODE_FAKE_VCS`                   | streng | Falsk VCS-leverandør for testformål                                   |
| `DEEPAGENT_CODE_CLIENT`                     | streng | Klientidentifikator (standard til `cli`)                              |
| `DEEPAGENT_CODE_ENABLE_EXA`                 | boolsk | Aktiver Exa-nettsøkeverktøy                                           |
| `DEEPAGENT_CODE_SERVER_PASSWORD`            | streng | Aktiver grunnleggende autentisering for `serve`/`web`                 |
| `DEEPAGENT_CODE_SERVER_USERNAME`            | streng | Overstyr grunnleggende autentiseringsbrukernavn (standard `deepagent-code`) |
| `DEEPAGENT_CODE_MODELS_URL`                 | streng | Egendefinert URL for henting av modellkonfigurasjon                   |

---

### Eksperimentelt

Disse miljøvariablene muliggjør eksperimentelle funksjoner som kan endres eller fjernes.

| Variabel                                        | Type   | Beskrivelse                                           |
| ----------------------------------------------- | ------ | ----------------------------------------------------- |
| `DEEPAGENT_CODE_EXPERIMENTAL`                         | boolsk | Aktiver eksperimentelle funksjoner under samleflagget |
| `DEEPAGENT_CODE_EXPERIMENTAL_ICON_DISCOVERY`          | boolsk | Aktiver ikonoppdagelse                                |
| `DEEPAGENT_CODE_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolsk | Deaktiver kopi ved valg i TUI                         |
| `DEEPAGENT_CODE_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | tall   | Standard tidsavbrudd for bash-kommandoer i ms         |
| `DEEPAGENT_CODE_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | tall   | Maks Output Tokens for LLM-svar                       |
| `DEEPAGENT_CODE_EXPERIMENTAL_FILEWATCHER`             | boolsk | Aktiver filovervåker for hele dir                     |
| `DEEPAGENT_CODE_EXPERIMENTAL_OXFMT`                   | boolsk | Aktiver oxfmt formatter                               |
| `DEEPAGENT_CODE_EXPERIMENTAL_LSP_TOOL`                | boolsk | Aktiver eksperimentelt LSP-verktøy                    |
| `DEEPAGENT_CODE_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolsk | Deaktiver filovervåking                               |
| `DEEPAGENT_CODE_EXPERIMENTAL_EXA`                     | boolsk | Aktiver eksperimentelle Exa-funksjoner                |
| `DEEPAGENT_CODE_EXPERIMENTAL_LSP_TY`                  | boolsk | Aktiver TY LSP for python-filer                       |
| `DEEPAGENT_CODE_EXPERIMENTAL_PLAN_MODE`               | boolsk | Aktiver planmodus                                     |
| `DEEPAGENT_CODE_EXPERIMENTAL_BACKGROUND_SUBAGENTS`    | boolsk | Aktiver bakgrunnsoppgaver for subagenter              |
| `DEEPAGENT_CODE_EXPERIMENTAL_EVENT_SYSTEM`            | boolsk | Aktiver eksperimentelt hendelsessystem                |
| `DEEPAGENT_CODE_EXPERIMENTAL_NATIVE_LLM`              | boolsk | Aktiver innebygd LLM-forespørselsvei                  |
| `DEEPAGENT_CODE_EXPERIMENTAL_PARALLEL`                | boolsk | Aktiver parallell kjøring av websøk                   |
| `DEEPAGENT_CODE_EXPERIMENTAL_SCOUT`                   | boolsk | Aktiver Scout-subagent                                |
| `DEEPAGENT_CODE_EXPERIMENTAL_WORKSPACES`              | boolsk | Aktiver arbeidsområde-støtte                          |
