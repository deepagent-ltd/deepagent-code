# TypeScript / JavaScript

## Boundary

TypeScript type modeling, JS/TS module interop, package-local typecheck, runtime boundary narrowing, and compiler diagnostics.

## Out of Scope

Framework-specific UI/backend/database behavior belongs to more specific packs. This pack also does not define React, API, database, or business rules; it handles TypeScript/JavaScript language and toolchain boundaries only.

## Applies When

Use this pack when task evidence, repository signals, or user intent matches: typescript, javascript. It contributes refs for max/ultra context admission only; it does not bypass runtime permissions, user approval, or project-specific instructions.

## Evidence Rules

All positive seed documents use medium evidence and must be checked against current repository evidence before use. TypeScript claims need package-local typecheck, package scripts, tsconfig/package.json evidence, compiler diagnostics, or generated type artifacts. Failure dossiers are diagnostic do-not-use signals and are intentionally excluded from index.json.

## Content Shape

Strategies: 9; methodologies: 6; knowledge: 9; skills: 3; failure dossiers: 6. Counts focus on package-local typecheck, module resolution, ESM/CJS interop, generics/inference, exports contracts, generated types, and Bun/npm/pnpm/yarn script behavior.

## L3 Validation

Representative activation and retrieval smoke lives in `evals/smoke/l3-smoke.json`. The quality report lives in `quality/l3-report.json`. A pack reaches L3 only when activation, pack-scoped retrieval, validation smoke, diagnosis smoke, and failure dossier boundaries are all present.
